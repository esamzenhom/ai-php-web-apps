# Auto detect text files and perform LF normalization
* text=auto

# Shell launchers must work even when Git for Windows uses autocrlf.
*.sh text eol=lf
*.command text eol=lf
*.py text eol=lf
*.ps1 text eol=crlf
*.cmd text eol=crlf
